# Local working record, box specifics, unanonymised notes.
phase-notes/
*-foundation.md
RETROSPECTIVE.md
HARVEST.md
.anonymize-list

# Credentials, never committed.
.env
*.pem
*.key
*.crt
credentials/

# Build scratch.
*.tar.gz
loreserver

# Generated media test corpus (test/make-corpus.sh recreates it).
corpus/

# The lore CLI creates a repository in the current directory.
# Guard against a stray one being committed; see docs/DEBUGGING.md.
.lore/
